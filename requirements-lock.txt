# Reference lock file — exact versions that satisfy requirements.txt as of the
# date below. Generated with:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#   pip freeze > requirements-lock.txt
#
# NOTE: this was generated in a clean sandbox on 2026-08-27, not captured from
# the original machine that produced the numbers in 05_results/. It pins a
# known-working combination for future reproduction; it does not certify that
# these exact versions were used for the dissertation's original run. If you
# need exact provenance for your own results, run the three commands above on
# the machine you use to (re)run the pipeline and keep that copy instead.
#
# Python 3.10.12 (this sandbox). requirements.txt is untested below Python 3.10.
certifi==2026.7.22
charset-normalizer==3.5.1
contourpy==1.3.2
cycler==0.12.1
fonttools==4.63.0
idna==3.19
kiwisolver==1.5.0
matplotlib==3.10.9
numpy==2.2.6
packaging==26.3
pandas==2.3.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
requests==2.34.2
scipy==1.15.3
six==1.17.0
tzdata==2026.3
urllib3==2.7.0
